# Skill Registry — commerce-x402

Auto-generated by sdd-init. Lists project conventions and matching user skills.

## Project Conventions

- `~/.claude/CLAUDE.md` — global user instructions (personality, language, philosophy, engram protocol, SDD orchestrator)

## Compact Rules

### From CLAUDE.md (global)

- Never use cat/grep/find/sed/ls — use bat/rg/fd/sd/eza
- Never build after changes
- Conventional commits only, no Co-Authored-By
- When asking a question, STOP and wait
- Verify before agreeing — say "dejame verificar"
- Engram persistent memory protocol is ALWAYS ACTIVE — mem_save proactively
- Strict TDD Mode: enabled

### SDD-specific (this project)

- Monorepo via pnpm workspaces + Turborepo
- Three packages: `@app/core` (domain, pure), `@app/web` (Next.js human UI), `@app/agent-api` (x402 endpoints)
- `@app/core` MUST NOT import from web/agent-api or use HTTP/Next.js primitives
- Shared DB (Postgres + Drizzle), single source of truth
- Base chain + USDC for both human and agent payments
- Identity deferred until E2E flows are green

## User Skills (relevant)

| Skill | Triggers | When to load |
|-------|----------|--------------|
| sdd-* | SDD workflow | All SDD phases |
| skill-creator | "create skill", "add skill" | Only if user asks |
| code-review | review diff | Pre-PR or on-demand |
| verify | manual verification | After implementing a feature |

## Notes

- Project is brand-new at init time — stack is *planned*, not detected from files.
- Re-run sdd-init after `pnpm install` to refresh capabilities from real package.json.
